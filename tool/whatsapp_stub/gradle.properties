org.gradle.jvmargs=-Xmx2G
org.gradle.java.home=/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home
# no AndroidX: the stub uses only the platform classes, which keeps the build
# to a single module with no dependencies to resolve
android.useAndroidX=false
