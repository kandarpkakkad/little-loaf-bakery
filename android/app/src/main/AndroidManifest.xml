<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- App lock. The device's own PIN or fingerprint; the app has no
         credential of its own. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Journey reminders. Every one is scheduled on this device from data it
         already holds; nothing is pushed and no server is involved. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Survive a reboot: a phone restarted overnight must still warn about
         the morning's deliveries. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- "30 minutes before" has to mean 30 minutes. USE_EXACT_ALARM is granted
         at install with no prompt; SCHEDULE_EXACT_ALARM would send the owner
         into a settings screen. Play restricts USE_EXACT_ALARM to apps whose
         job is alarms — this app is side-loaded from GitHub Releases, so that
         policy does not apply to it. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:label="Little Loaf"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:enableOnBackInvokedCallback="true"
            android:launchMode="singleTop"
            android:resizeableActivity="true"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Reschedules the journey reminders after a reboot. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- Android 11 package visibility. Without these the system will not
             tell the app that anything can handle a link, so url_launcher
             finds no activity and simply returns false: the WhatsApp sheet
             closed with nothing happening, "Open Maps" did nothing, tapping a
             customer's number did nothing, and the update screen's Download
             button did nothing. Every outbound link in the app went through
             this hole.

             One entry per scheme the app actually launches, and no more —
             this is a visibility declaration, not a permission. -->
        <intent>
            <!-- wa.me for WhatsApp, Google Maps search, the release download -->
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <!-- tapping a customer's number -->
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <!-- WhatsApp's own scheme, the fallback when the https link
                 resolves to nothing -->
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="whatsapp"/>
        </intent>
        <intent>
            <!-- a pin, where one was captured -->
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="geo"/>
        </intent>
    </queries>
</manifest>
